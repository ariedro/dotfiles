#!/bin/bash

HOURS="$HOME/notes/$(date -I).txt"
if [ ! -d "$HOME/notes" ]; then mkdir "$HOME/notes"; fi
if [ ! -f "$HOURS" ]; then touch $HOURS; fi

hour=$(date -ud @$(($(date +%s) - $(date -d "$(uptime -s)" +%s))) +%H:%M)
echo -n "$hour: " >> $HOURS
nano $HOURS